<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="memberDAO">
	<!-- 아이디 찾기 -->	
	<select id="idCheck" resultType="Integer">
		select count(*) from member where id=#{id}
	</select>
	
	<!-- 로그인 비번 확인 -->
	<select id="getPass" resultType="String">
		select pw from member where id=#{id}
	</select>
	
	<!-- 포지션 찾기 -->
	<select id="getBrName" resultType="String">
		select branchName from member where id=#{id}
	</select>
	
	<!-- 회원가입 -->
	<insert id="insertMember">
		insert into member
		<if test="kid==''and branchName==null">
			(id,pw,name,phone,birth,email) values(#{id},#{pw},#{name},#{phone},#{birth},#{email})
		</if>
		<if test="kid!=''and branchName==null">
			(id,pw,name,phone,birth,email) values(#{kid},#{pw},#{name},#{phone},#{birth},#{email})
		</if> 
		<if test="branchName!=null">
			values(#{id},#{pw},#{name},#{phone},#{birth},#{email},#{branchName},#{zipcode},#{branchAddr1},#{branchAddr2},'지점장',-1)
		</if>
	</insert>

</mapper>