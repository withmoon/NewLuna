<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="memberDAO">
	
	<select id="idCheck" resultType="Integer">
		select count(*) from member where id=#{id}
	</select>
	
	<insert id="insertMember">
		insert into member
		<if test="kid==''">
			(id,pw,name,phone,birth,email) values(#{id},#{pw},#{name},#{phone},#{birth},#{email})
		</if>
		<if test="kid!=''">
			(id,pw,name,phone,birth,email) values(#{kid},#{pw},#{name},#{phone},#{birth},#{email})
		</if> 
		<if test="branchName!=null">
			values(#{id},#{pw},#{name},#{phone},#{birth},#{email},#{branchName},#{zipcode},#{branchAddr1},#{branchAddr2}#{branchAddr3},'지점장',-1)
		</if>
	</insert>

</mapper>